#include <stdio.h>

int main() {
    int i;
    for (=3,i <=12;) {
        printf("%d " , i);
    }
    printf("\nEnd of loop\n");
    return 0;
}

//for loop: ใช้ range(15,40,20) เพื่อวนลูปตั้งแต่ 3 ถึง 12